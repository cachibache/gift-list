<%= form_for [@list, @gift] do |f| %>

  <%= f.label :name, "Gift name" %>:
  <%= f.text_field :name %><br/>

  <%= f.label :description, "Gift details or notes" %>:
  <%= f.text_field :description %><br/>

  <%= f.hidden_field :list_id, :value => params[:list_id] %>
  
  <%= f.submit %>
<% end %>